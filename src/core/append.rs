use super::*; 

impl <F, S> BiVec<F, S> {
    /// Appends the contents of `other` to `self`. 
    /// 
    /// # UNIMPLEMENTED 
    pub fn append(&mut self, other: &mut BiVec<F, S>) {
        unimplemented!() 
    }
}