[package]
name = "bivec"
version = "0.1.0"
edition = "2021"
description = "A Rust implementation of Bivec, two vecs in one, with the same length. "
license = "MIT OR Apache-2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
